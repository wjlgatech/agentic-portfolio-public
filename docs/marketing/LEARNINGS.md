# Marketing learnings — earned, not invented

The compounding half of the loop. A **RULE** needs ≥3 posts of evidence (cited by
ledger id) and constrains every future draft; below that it's a **HYPOTHESIS** with a
named next test. Prune rules the moment the ledger contradicts them.

## Rules (active — every MAKE must obey)

_(none yet — a rule is promoted only on ≥3 posts of evidence)_

## Hypotheses (test next)

- HYPOTHESIS (n=0): The "dead PDF" pain-hook outperforms a feature-hook ("AI portfolio
  in 60s") for the ICP — test by: ship `POST-RESUME-IS-DEAD.md` (pain-hook), then a
  feature-hook variant of the same CTA, compare comment-rate at 48h.
- HYPOTHESIS (n=0): A one-word CTA ("comment TRUE") beats a link-only CTA for
  qualified replies — test by: A/B across the article post and the next wedge post.
